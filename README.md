# Draw-Steel-Adventure-Noticeboard
Student Project to make a working noticeboard for the game Draw Steel

learning about many to many paradigm in database structures like postgres and postgresql that prevents arrays of foreign keys and instead uses 2 tables and concatinates them.

learning about env variables and storing them in a .env file then loading with os.getenv()

